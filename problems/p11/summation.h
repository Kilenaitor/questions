#include <vector>
static std::vector<std::pair<int, int> > summation(std::vector<int> &v, int n)
{
	/* code goes here */
	(void)n;
	(void)v;
	std::pair<int, int> p(0, 0);
	std::vector<std::pair<int, int> > ans {p};
	return ans;
}